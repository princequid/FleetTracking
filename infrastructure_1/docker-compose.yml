# ===========================================================================
# FleetTrack Pro — Docker Compose
# ===========================================================================
# Start order:
#   1. Infrastructure  — postgres, rabbitmq, redis, minio, osrm
#   2. Eureka Server   — service registry (waits for postgres healthcheck)
#   3. Auth Service    — JWT auth (waits for postgres + eureka)
#   4. API Gateway     — single entry point (waits for eureka + auth)
#   5. Business services — driver, vehicle, trip, etc. (wait for postgres + eureka)
#
# Run:  docker compose up -d
# Logs: docker compose logs -f
# Stop: docker compose down
# ===========================================================================

services:

  # ── Infrastructure ─────────────────────────────────────────────────────

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-fleettrack}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-fleettrack}
      POSTGRES_DB: ${POSTGRES_DB:-fleettrack}
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./db/init:/docker-entrypoint-initdb.d
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fleettrack -d fleettrack"]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: "0.5"
    restart: unless-stopped

  rabbitmq:
    image: rabbitmq:3.12-management-alpine
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER:-fleettrack}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD:-fleettrack}
    ports:
      - "5672:5672"
      - "15672:15672"
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: "0.5"
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          memory: 64M
          cpus: "0.25"
    restart: unless-stopped

  minio:
    image: minio/minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-fleettrack}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-fleettrack123}
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio-data:/data
    deploy:
      resources:
        limits:
          memory: 128M
          cpus: "0.25"
    restart: unless-stopped

  osrm:
    image: osrm/osrm-backend
    command: osrm-routed --algorithm mld --max-table-size 1000 /data/ghana-latest.osrm
    volumes:
      - ./osrm-data:/data
    ports:
      - "5000:5000"
    deploy:
      resources:
        limits:
          memory: 128M
          cpus: "0.25"
    restart: unless-stopped

  # ── Eureka Server ──────────────────────────────────────────────────────

  eureka-server:
    build:
      context: ../backend/eureka-server_1
      dockerfile: Dockerfile
    ports:
      - "8761:8761"
    environment:
      JAVA_OPTS: "-Xmx256m -Xms128m"
    depends_on:
      postgres:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 384M
          cpus: "0.5"
    restart: unless-stopped

  # ── Auth Service ───────────────────────────────────────────────────────

  auth-service:
    build:
      context: ../backend
      dockerfile: auth-service_1/Dockerfile
    # SECURITY: internal-only. Reached exclusively through the API gateway over the
    # compose network — NOT published to the host, so clients can't bypass the
    # gateway (and its JWT check) to hit the service directly.
    expose:
      - "8081"
    environment:
      INTERNAL_SERVICE_SECRET: ${INTERNAL_SERVICE_SECRET:-fleettrack-internal-dev-secret}
      JWT_SECRET: ${JWT_SECRET:-dev-only-change-this-to-a-long-random-string-before-prod}
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/fleettrack
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER:-fleettrack}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD:-fleettrack}
      SPRING_RABBITMQ_HOST: rabbitmq
      SPRING_RABBITMQ_USERNAME: ${RABBITMQ_USER:-fleettrack}
      SPRING_RABBITMQ_PASSWORD: ${RABBITMQ_PASSWORD:-fleettrack}
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka-server:8761/eureka/
      JAVA_OPTS: "-Xmx384m -Xms128m"
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      eureka-server:
        condition: service_started
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "0.5"
    restart: unless-stopped

  # ── API Gateway ────────────────────────────────────────────────────────

  api-gateway:
    build:
      context: ../backend/api-gateway_1
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      INTERNAL_SERVICE_SECRET: ${INTERNAL_SERVICE_SECRET:-fleettrack-internal-dev-secret}
      JWT_SECRET: ${JWT_SECRET:-dev-only-change-this-to-a-long-random-string-before-prod}
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka-server:8761/eureka/
      JAVA_OPTS: "-Xmx256m -Xms128m"
    depends_on:
      eureka-server:
        condition: service_started
      auth-service:
        condition: service_started
    deploy:
      resources:
        limits:
          memory: 384M
          cpus: "0.5"
    restart: unless-stopped

  # ── Business Services ──────────────────────────────────────────────────

  driver-service:
    build:
      context: ../backend/driver-service_1
      dockerfile: Dockerfile
    # SECURITY: internal-only — reachable via the gateway, not published to the host.
    expose:
      - "8082"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/fleettrack
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER:-fleettrack}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD:-fleettrack}
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka-server:8761/eureka/
      JAVA_OPTS: "-Xmx384m -Xms128m"
    depends_on:
      postgres:
        condition: service_healthy
      eureka-server:
        condition: service_started
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "0.5"
    restart: unless-stopped

  vehicle-service:
    build:
      context: ../backend/vehicle-service_1
      dockerfile: Dockerfile
    # SECURITY: internal-only — reachable via the gateway, not published to the host.
    expose:
      - "8083"
    environment:
      INTERNAL_SERVICE_SECRET: ${INTERNAL_SERVICE_SECRET:-fleettrack-internal-dev-secret}
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/fleettrack
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER:-fleettrack}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD:-fleettrack}
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka-server:8761/eureka/
      JAVA_OPTS: "-Xmx384m -Xms128m"
    depends_on:
      postgres:
        condition: service_healthy
      eureka-server:
        condition: service_started
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "0.5"
    restart: unless-stopped

  trip-service:
    build:
      context: ../backend
      dockerfile: trip-service_2/Dockerfile
    # SECURITY: internal-only — reachable via the gateway, not published to the host.
    expose:
      - "8084"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/fleettrack
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER:-fleettrack}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD:-fleettrack}
      SPRING_RABBITMQ_HOST: rabbitmq
      SPRING_RABBITMQ_USERNAME: ${RABBITMQ_USER:-fleettrack}
      SPRING_RABBITMQ_PASSWORD: ${RABBITMQ_PASSWORD:-fleettrack}
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka-server:8761/eureka/
      JAVA_OPTS: "-Xmx384m -Xms128m"
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      eureka-server:
        condition: service_started
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "0.5"
    restart: unless-stopped

  gps-service:
    build:
      context: ../backend
      dockerfile: gps-service_2/Dockerfile
    # SECURITY: internal-only — reachable via the gateway, not published to the host.
    expose:
      - "8085"
    environment:
      INTERNAL_SERVICE_SECRET: ${INTERNAL_SERVICE_SECRET:-fleettrack-internal-dev-secret}
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/fleettrack
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER:-fleettrack}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD:-fleettrack}
      SPRING_DATA_REDIS_HOST: redis
      SPRING_RABBITMQ_HOST: rabbitmq
      SPRING_RABBITMQ_USERNAME: ${RABBITMQ_USER:-fleettrack}
      SPRING_RABBITMQ_PASSWORD: ${RABBITMQ_PASSWORD:-fleettrack}
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka-server:8761/eureka/
      JAVA_OPTS: "-Xmx384m -Xms128m"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      eureka-server:
        condition: service_started
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "0.5"
    restart: unless-stopped

  incident-service:
    build:
      context: ../backend
      dockerfile: incident-service_3/Dockerfile
    # SECURITY: internal-only — reachable via the gateway, not published to the host.
    expose:
      - "8087"
    environment:
      INTERNAL_SERVICE_SECRET: ${INTERNAL_SERVICE_SECRET:-fleettrack-internal-dev-secret}
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/fleettrack
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER:-fleettrack}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD:-fleettrack}
      SPRING_RABBITMQ_HOST: rabbitmq
      SPRING_RABBITMQ_USERNAME: ${RABBITMQ_USER:-fleettrack}
      SPRING_RABBITMQ_PASSWORD: ${RABBITMQ_PASSWORD:-fleettrack}
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka-server:8761/eureka/
      JAVA_OPTS: "-Xmx384m -Xms128m"
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      eureka-server:
        condition: service_started
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "0.5"
    restart: unless-stopped

  media-service:
    build:
      context: ../backend/media-service_3
      dockerfile: Dockerfile
    # SECURITY: internal-only — reachable via the gateway, not published to the host.
    expose:
      - "8086"
    environment:
      INTERNAL_SERVICE_SECRET: ${INTERNAL_SERVICE_SECRET:-fleettrack-internal-dev-secret}
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/fleettrack
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER:-fleettrack}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD:-fleettrack}
      MINIO_ENDPOINT: http://minio:9000
      # MUST be the dev machine's LAN IP (the value the phone uses for the API), NOT
      # localhost — the presigned upload URL is opened by the phone, not the server.
      MINIO_EXTERNAL_ENDPOINT: http://172.20.10.4:9000
      MINIO_ACCESS_KEY: ${MINIO_ROOT_USER:-fleettrack}
      MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-fleettrack123}
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka-server:8761/eureka/
      JAVA_OPTS: "-Xmx384m -Xms128m"
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_started
      eureka-server:
        condition: service_started
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "0.5"
    restart: unless-stopped

  audit-service:
    build:
      context: ../backend
      dockerfile: audit-service/Dockerfile
    # SECURITY: internal-only — reachable via the gateway, not published to the host.
    expose:
      - "8090"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/fleettrack
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER:-fleettrack}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD:-fleettrack}
      SPRING_RABBITMQ_HOST: rabbitmq
      SPRING_RABBITMQ_USERNAME: ${RABBITMQ_USER:-fleettrack}
      SPRING_RABBITMQ_PASSWORD: ${RABBITMQ_PASSWORD:-fleettrack}
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka-server:8761/eureka/
      JAVA_OPTS: "-Xmx384m -Xms128m"
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      eureka-server:
        condition: service_started
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "0.5"
    restart: unless-stopped

  trip-service:
    build:
      context: ../backend
      dockerfile: trip-service_2/Dockerfile
    ports:
      - "8084:8084"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/fleettrack
      SPRING_DATASOURCE_USERNAME: fleettrack
      SPRING_DATASOURCE_PASSWORD: fleettrack
      SPRING_RABBITMQ_HOST: rabbitmq
      SPRING_RABBITMQ_USERNAME: fleettrack
      SPRING_RABBITMQ_PASSWORD: fleettrack
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka-server:8761/eureka/
      JAVA_OPTS: "-Xmx384m -Xms128m"
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      eureka-server:
        condition: service_started
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "0.5"
    restart: unless-stopped

  gps-service:
    build:
      context: ../backend/gps-service_2
      dockerfile: Dockerfile
    ports:
      - "8085:8085"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/fleettrack
      SPRING_DATASOURCE_USERNAME: fleettrack
      SPRING_DATASOURCE_PASSWORD: fleettrack
      SPRING_DATA_REDIS_HOST: redis
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka-server:8761/eureka/
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      eureka-server:
        condition: service_started
    restart: unless-stopped

volumes:
  postgres-data:
  minio-data:
