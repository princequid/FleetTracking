server:
  shutdown: graceful
  port: 8761

spring:
  lifecycle:
    timeout-per-shutdown-phase: 30s
  application:
    name: eureka-server

eureka:
  instance:
    hostname: localhost
  client:
    # This IS the registry — it doesn't need to register with itself
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/

logging:
  level:
    com.netflix.eureka: WARN
    com.netflix.discovery: WARN
